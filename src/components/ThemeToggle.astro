---
// Theme toggle removed as per user request to force light mode
---
<script is:inline>
  // Force light mode and clear any previous user preference for other modes
  document.documentElement.dataset.theme = 'light';
  localStorage.setItem('theme', 'light');
</script>
